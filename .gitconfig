[user]
	email = rkochp@ext.uber.com
	name = rkochp
[core]
	editor = vim
[alias]
	s = status
	l = log --pretty=format:'%C(bold yellow)%h%Creset %Cgreen%ad %C(bold)(%ar) %Creset%s %Cblue(%an)' --date=short
	ls = l -n5
	lg1 = log --graph --abbrev-commit --decorate --format=format:'%C(bold yelow)%h%C(reset) - %C(bold green)(%ar)%C(reset) %C(white)%s%C(reset) %C(dim white)- %an%C(reset)%C(auto)%d%C(reset)' --all
	lg2 = log --graph --abbrev-commit --decorate --format=format:'%C(bold yelow)%h%C(reset) - %C(bold cyan)%aD%C(reset) %C(bold green)(%ar)%C(reset)%C(auto)%d%C(reset)%n''          %C(white)%s%C(reset) %C(dim white)- %an%C(reset)'
	lg = lg1
	br = branch
	ci = commit
	co = checkout
	f = fetch
	m = merge
	rb = rebase
	st = stash
	cp = cherry-pick 
[http "https://gopkg.uberinternal.com"]
	followRedirects = true
[http "https://gopkg.in"]
	followRedirects = true

# Uber-related
[farc "upload"]
    add-repo-prefix = false

[url "ssh://code.uber.internal/"]
	insteadOf = https://code.uber.internal/
[url "ssh://config.uber.internal/"]
	insteadOf = https://config.uber.internal/
[url "ssh://gitolite@code.uber.internal/"]
	insteadOf = git://code.uber.internal/
[url "ssh://git@github.com/uber/"]
	insteadOf = https://github.com/uber/
[credential "https://git.uberinternal.com"]
	helper = usso
[credential "https://vfs4g-cache.uberinternal.com"]
	helper = usso
